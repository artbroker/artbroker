[build]
  command = "npm run build"
  publish = "dist"

[dev]
  command = "npm run dev:astro -- --host 127.0.0.1"
  targetPort = 4321
  port = 8888
  framework = "#custom"

[functions]
  directory = "netlify/functions"
